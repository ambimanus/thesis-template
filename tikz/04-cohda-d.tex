\begin{tikzpicture}[
  scale=0.9657,
  decoration={
      markings,
      mark=at position 1 with {\arrow[scale=1,primary]{latex}};
    },
  ]
  % \draw [help lines] (0,0) grid (4,4);
  % \fill [red] (0,0) circle (2pt);
  %
  \def\maxnode{7}
  % Nodes
  \node[mynode] at ($(0,0)!1!{360-0*360/8}:(0,\getvalue{nradius})$) (a0) {\xsverbundsp[0]};
  \foreach \i in {1,2,...,\maxnode} {
    \node[mynode] at ($(0,0)!1!{360-\i*360/8}:(0,\getvalue{nradius})$) (a\i) {\xsverbundtn[\i]};
  }
  % Select background layer
  \begin{pgfonlayer}{bg}
    \pgfmathtruncatemacro{\endring}{\maxnode-1)}
    \foreach \i in {1,2,...,\endring} {
      % Lines (circle)
      \pgfmathtruncatemacro{\j}{mod((\i+1),(\maxnode+1))}
      \draw[myline] (a\i.center) -- (a\j.center);
    }
    % Lines (shortcuts)
    \foreach \i/\j in {1/3, 1/4, 1/5, 3/5, 4/6} {
      \draw[myline] (a\i.center) -- (a\j.center);
    }
    % Messages
    \draw[mymsg] (a0) -- node[above] {\xsarbeitsged[0]} (a1);
    \draw[mymsg] (a0) -- node[above] {\xsarbeitsged[0]} (a7);
    \draw[mymsg] (a0) -- node[below=1mm, pos=0.05] {\xsarbeitsged[0]} (a2);
    \draw[mymsg] (a0) -- node[above left=1mm and -2mm, near start] {\xsarbeitsged[0]} (a5);
  \end{pgfonlayer}
\end{tikzpicture}